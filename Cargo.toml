[package]
name = "disk-cleaner"
version = "0.1.0"
edition = "2024"
description = "A fast disk cleanup tool written in Rust"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
walkdir = "2"
colored = "3"
anyhow = "1"
